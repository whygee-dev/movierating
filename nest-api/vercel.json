{
  "version": 2,
  "env": {
    "DATABASE_URL": "@DATABASE_URL",
    "jwtSecret": "@jwtSecret",
    "TMDB_KEY": "@TMDB_KEY"
  },
  "builds": [
    {
      "src": "src/main.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/main.ts",
      "methods": ["GET", "POST", "PUT", "DELETE"]
    }
  ]
}
